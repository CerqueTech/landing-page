---
interface Props {
  text_one: string;
  text_two: string;
  text_three: string;
}

const { text_one, text_two, text_three } = Astro.props;
---

<section>
  <h1 class="first-row">{text_one}</h1>
  <h1 class="second-row">
    {text_two}<img
      class="hourglass"
      src="../../public/svg/hourglass.svg"
      alt="hourglass"
    />{text_three}
  </h1>
</section>
<footer>
  <p>Cerquetech Solutions®</p>
</footer>

<style>
  section {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 5rem;
    text-align: center;
  }

  .first-row,
  .second-row {
    letter-spacing: -0.4rem;
    margin-top: 0;
    margin-bottom: 0;
    font-family: Silkscreen, sans-serif;
    font-size: 5rem;
    font-weight: 400;
    line-height: 1.1;
    white-space: nowrap;
  }

  .second-row {
    position: relative;
    left: 140px;
  }

  .hourglass {
    width: 1em;
    height: 1em;
    vertical-align: middle;
    animation: spin 2s linear infinite;
  }

  footer {
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    text-align: center;
    width: 100%;
    padding: 1rem;
    font-size: 1.2rem;
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @media (max-width: 768px) {
    .first-row,
    .second-row {
      font-size: 3rem;
    }
    .second-row {
      left: 80px;
    }
  }

  @media (max-width: 480px) {
    .first-row,
    .second-row {
      font-size: 2rem;
    }
    .second-row {
      left: 40px;
    }
  }
</style>
